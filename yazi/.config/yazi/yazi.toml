# Add these sections to your default yazi.toml
# Only including the safe, commonly-used settings from your old config
[mgr]
ratio          = [ 1, 4, 3 ]           # Your custom pane ratios
sort_by        = "alphabetical"
sort_sensitive = false
sort_reverse   = false
sort_dir_first = true
show_hidden    = true                   # Show hidden files
show_symlink   = true
scrolloff      = 5                      # Keep 5 lines visible when scrolling
title_format   = "Yazi: {cwd}"

[preview]
tab_size        = 2
max_width       = 600                   # Your custom preview sizes
max_height      = 900
image_quality   = 75

[opener]
edit = [
  { run = 'nvim "$@"', block = true, for = "unix" },
]
image = [
  { run = 'swayimg "$@"', orphan = true, for = "unix" },
]
play = [
  # { run = 'mpv --force-window "$@"', orphan = true, for = "unix" },
  { run = 'vlc "$@"', orphan = true, for = "unix" },
]

[open]
rules = [
  { name = "*/", use = [ "edit", "open", "reveal" ] },
  { mime = "text/*", use = [ "edit", "reveal" ] },
  { mime = "image/*", use = [ "image" ] },
  { mime = "{audio,video}/*", use = [ "play", "reveal" ] },
  { mime = "application/{json,ndjson}", use = [ "edit", "reveal" ] },
  { mime = "*/javascript", use = [ "edit", "reveal" ] },
  { name = "*", use = [ "open", "reveal" ] },
]

[input]
cursor_blink = false

# Your custom input dialog positions
cd_title  = "Change directory:"
cd_origin = "top-center"
cd_offset = [ 0, 2, 50, 3 ]

create_title  = [ "Create:", "Create (dir):" ]
create_origin = "top-center"
create_offset = [ 0, 2, 50, 3 ]

rename_title  = "Rename:"
rename_origin = "hovered"
rename_offset = [ 0, 1, 50, 3 ]

filter_title  = "Filter:"
filter_origin = "top-center"
filter_offset = [ 0, 2, 50, 3 ]

find_title  = [ "Find next:", "Find previous:" ]
find_origin = "top-center"
find_offset = [ 0, 2, 50, 3 ]

search_title  = "Search via {n}:"
search_origin = "top-center"
search_offset = [ 0, 2, 50, 3 ]

shell_title  = [ "Shell:", "Shell (block):" ]
shell_origin = "top-center"
shell_offset = [ 0, 2, 50, 3 ]

[confirm]
trash_title   = "Trash {n} selected file{s}?"
trash_origin  = "center"
trash_offset  = [ 0, 0, 70, 20 ]

delete_title  = "Permanently delete {n} selected file{s}?"
delete_origin = "center"
delete_offset = [ 0, 0, 70, 20 ]

quit_title   = "Quit?"
quit_content = "The following tasks are still running, are you sure you want to quit?"
quit_origin  = "center"
quit_offset  = [ 0, 0, 50, 15 ]
